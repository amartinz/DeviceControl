include ':app'
include ':libs:mpandroidchart:MPChartLib'
include ':libs:AndroidSwipeLayout:library'

def String localProps = System.getenv('USE_LOCAL_MATERIALPREFERENCES_SOURCES')
if (localProps != null) {
    include ':locallibs:MaterialPreferences:library'
}
